<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_line_tree_inherit" model="ir.ui.view">
        <field name="name">view.move.line.tree.inherit</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_stock_move_line_operation_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='result_package_id' and not(@column_invisible='True')]" position="before">
                <field name="package_id" column_invisible="parent.picking_type_id == 1"/>
            </xpath>
            <xpath expr="//field[@name='result_package_id' and not(@column_invisible='True')]" position="attributes">
                <attribute name="readonly">validation_state != 'pending'</attribute>
            </xpath>
            <xpath expr="//field[@name='quantity']" position="before">
                <!-- <field name="tara_bolsa" column_invisible="parent.picking_type_id != 1"/>
                <field name="cantidad_conos" column_invisible="parent.picking_type_id != 1"/>
                <field name="tara_cono" column_invisible="parent.picking_type_id != 1"/>
                <field name="peso_bruto" column_invisible="parent.picking_type_id != 1"/>
                <field name="peso_neto" column_invisible="parent.picking_type_id != 1"/> -->
                <field name="package_type_id" readonly="validation_state != 'pending'"/>
                <field name="tara_bolsa" readonly="validation_state != 'pending'"/>
                <field name="cono_id" readonly="validation_state != 'pending'"/>
                <field name="cantidad_conos" readonly="validation_state != 'pending'"/>
                <field name="tara_cono" readonly="validation_state != 'pending'"/>
                <field name="tara_cono_total" readonly="validation_state != 'pending'"/>
                <field name="peso_bruto" readonly="validation_state != 'pending'"/>
                <field name="peso_neto" readonly="validation_state != 'pending'"/>
            </xpath>
            <xpath expr="//field[@name='quantity']" position="attributes">
                <attribute name="optional">hide</attribute>
            </xpath>
            <xpath expr="//list" position="inside">
                <field name="validation_state" widget="badge"
                       decoration-success="validation_state == 'validated'"
                       decoration-warning="validation_state == 'pending'"
                       decoration-danger="validation_state == 'cancel'"/>
                <button name="action_duplicar" string="Duplicar" type="object"
                        column_invisible="parent.state in ('done', 'cancel')"/>
                <button name="action_validate_line" string="Validar" type="object" icon="fa-check"
                        invisible="validation_state != 'pending'"
                        column_invisible="parent.state in ('done', 'cancel')"/>
                <button name="action_print_label" string="Imprimir ZPL" type="object" icon="fa-print"
                        invisible="validation_state != 'validated'"/>
                <button name="action_reset_line" string="Reabrir" type="object" icon="fa-undo"
                        invisible="validation_state == 'pending'"
                        column_invisible="parent.state in ('done', 'cancel')"/>
                <button name="action_cancel_line" string="Cancelar" type="object" icon="fa-times"
                        invisible="validation_state != 'pending'"
                        column_invisible="parent.state in ('done', 'cancel')"/>
            </xpath>

        </field>
    </record>

    <record id="view_move_line_detailed_tree_inherit" model="ir.ui.view">
        <field name="name">stock.move.line.detailed.operation.tree.inherit</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_stock_move_line_detailed_operation_tree"/>
        <field name="arch" type="xml">

             <xpath expr="//field[@name='result_package_id' and not(@column_invisible='True')]" position="before">
                <field name="package_id"/>
            </xpath>
            <xpath expr="//field[@name='result_package_id' and not(@column_invisible='True')]" position="attributes">
                <attribute name="readonly">validation_state != 'pending'</attribute>
            </xpath>
            <xpath expr="//field[@name='quantity']" position="before">
                <field name="package_type_id" readonly="validation_state != 'pending'"/>
                <field name="tara_bolsa" readonly="validation_state != 'pending'"/>
                <field name="cono_id" readonly="validation_state != 'pending'"/>
                <field name="cantidad_conos" readonly="validation_state != 'pending'"/>
                <field name="tara_cono" readonly="validation_state != 'pending'"/>
                <field name="tara_cono_total" readonly="validation_state != 'pending'"/>
                <field name="peso_bruto" readonly="validation_state != 'pending'"/>
                <field name="peso_neto" readonly="validation_state != 'pending'"/>
            </xpath>
            <xpath expr="//field[@name='quantity']" position="attributes">
                <attribute name="optional">hide</attribute>
            </xpath>
            <xpath expr="//list" position="inside">
                <field name="validation_state" widget="badge"
                       decoration-success="validation_state == 'validated'"
                       decoration-warning="validation_state == 'pending'"
                       decoration-danger="validation_state == 'cancel'"/>
                <button name="action_validate_line" string="Validar" type="object" icon="fa-check"
                        invisible="validation_state != 'pending'"
                        column_invisible="parent.state in ('done', 'cancel')"/>
                <button name="action_print_label" string="Imprimir ZPL" type="object" icon="fa-print"
                        invisible="validation_state != 'validated'"/>
                <button name="action_reset_line" string="Reabrir" type="object" icon="fa-undo"
                        invisible="validation_state == 'pending'"
                        column_invisible="parent.state in ('done', 'cancel')"/>
                <button name="action_cancel_line" string="Cancelar" type="object" icon="fa-times"
                        invisible="validation_state != 'pending'"
                        column_invisible="parent.state in ('done', 'cancel')"/>
            </xpath>
        </field>
    </record>
</odoo>
